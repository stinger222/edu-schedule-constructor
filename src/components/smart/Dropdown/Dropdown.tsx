import { StyledDropdown } from "./Dropdown.styled"

interface IProps {
	
}

const Dropdown: React.FC<IProps> = ({  }) => {
	return (
		<StyledDropdown>
			
		</StyledDropdown>
	)
}

export default Dropdown
