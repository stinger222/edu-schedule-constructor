import styled from "styled-components";

export const StyledAddComposedSchedule = styled.div`
	& form > h2 {
		text-align: center;
		font-weight: 500;
		margin-bottom: 0.7em;
	}

	& form > .btn-ghost {
		width: 38em;
		padding: 6em 0;
	}

	& form > .btn-ghost > span {
		line-height: 2.2em;
		font-size: 1.6em;
	}
`