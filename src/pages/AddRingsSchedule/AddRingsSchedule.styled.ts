import styled from "styled-components";

export const StyledAddRingsSchedule = styled.div`
	& .btn {
		display: block;
		margin-inline: auto;
	}

	& .append-range {
		font-size: 1.3em;
		margin-top: 1em;
	}

	& .submit {
		margin-top: 2em;
		font-size: 1.5em;
	}
`