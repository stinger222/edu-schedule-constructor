export interface ITheme {
	colors: {
		bg: string,
		cardBg: string,
		buttonBg: string,

		primary: string,
		secondary: string,

		textPrimary: string,
		textSecondary: string
	},
	boxShadows: {
		primaryShadow: string,
		secondaryShadow: string
	}
}